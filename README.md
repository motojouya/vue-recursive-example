# vue-recursive-example

## how to use
do this

```shell
docker build -t you-repository:vue-recursive-example .
docker run -it -v /path/to/current/directory:/usr/local/src/vue-recursive-example you-repository:vue-recursive-example bash
yarn install
npm start
```

